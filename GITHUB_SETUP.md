# GitHub setup

```bash
git init
git add .
git commit -m "Initial FRDG compliance intelligence prototype"
git branch -M main
git remote add origin https://github.com/Nancy-05-Srivastava/Graph-Based-Impact-Analysis.git
git push -u origin main
```

Recommended repository description:

> Graph-Based Operational Compliance Intelligence for Financial Institutions — an explainable Financial Regulatory Dependency Graph (FRDG) prototype for multi-hop regulatory impact analysis.

Recommended topics:

`fintech` `regtech` `compliance` `knowledge-graph` `graphrag` `graph-neural-networks` `financial-regulation` `fastapi` `react` `cytoscape` `ai`
